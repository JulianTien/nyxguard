# NyxGuard 整体架构图

本文档基于当前仓库实际实现整理，口径以 `app/` Android 客户端与 `backend/` FastAPI 服务为准。

## 1. 系统整体架构

```mermaid
flowchart LR
    user["用户 / 守护者"] --> android

    subgraph android["Android 客户端（app/）"]
        ui["UI 与交互层\nMainActivity / Home / Map / Chat / Profile\nWalk / Ride / Fake Call / Alarm"]
        feature["业务功能层\nWalkTrackingActivity\nRideTrackingActivity\nGuardian / SOS / Fake Call"]
        service["设备能力与本地状态\nLocationTrackingService\nActiveTripStore\nPendingLocationStore（Room）"]
        network["网络访问层\nApiClient / ApiService\nRetrofit + OkHttp + Gson\nJWT TokenManager"]
        amap["地图与定位 SDK\nAMap Map / Search / Location"]

        ui --> feature
        feature --> service
        feature --> network
        feature --> amap
        service --> network
    end

    subgraph backend["FastAPI 后端（backend/）"]
        entry["应用入口\nmain.py / server.py\ncreate_app()"]
        router["API 路由层\n/auth /user /guardians /trips\n/notifications /chat /v2"]
        domain["领域服务层\nJWT 鉴权 deps.py\nAI 守护 ai_guardian.py\n通知事件 notification_events.py"]
        persistence["数据访问层\nSQLAlchemy models / schemas / database"]

        entry --> router
        router --> domain
        router --> persistence
        domain --> persistence
    end

    subgraph data["数据与外部依赖"]
        db["业务数据库\nNeon Postgres（生产）\nSQLite fallback（本地开发/演示）"]
        openai["OpenAI Responses API\nAI 对话生成"]
        vercel["Vercel\n官网 / 静态内容 / 文档站 / 演示页"]
    end

    network -->|HTTPS JSON API| backend
    persistence --> db
    domain -->|有网络且已配置 OPENAI_API_KEY| openai
    backend -.独立部署.-> vercel
```

## 2. 核心链路说明

### 2.1 步行 / 乘车守护链路

```mermaid
flowchart LR
    setup["WalkSettingActivity / RideSettingActivity"] --> tracking["WalkTrackingActivity / RideTrackingActivity"]
    tracking --> fg["LocationTrackingService\n前台定位服务"]
    fg --> cache["PendingLocationStore\nRoom 离线缓存/补传"]
    tracking --> api1["POST /api/trips"]
    fg --> api2["POST /api/trips/{id}/locations"]
    tracking --> api3["POST /api/v2/trips/{id}/alerts\nPUT /api/trips/{id}/finish"]
    api1 --> fastapi["FastAPI routers"]
    api2 --> fastapi
    api3 --> fastapi
    fastapi --> tripdb["ng_trip / ng_location / ng_notification_event"]
```

### 2.2 AI 关怀对话链路

```mermaid
flowchart LR
    chat["ChatFragment"] --> v2chat["/api/v2/chat/messages"]
    v2chat --> ai["ai_guardian.py"]
    ai -->|优先| llm["OpenAI Responses API"]
    ai -->|降级| local["本地兜底回复"]
    ai --> msgdb["ng_chat_message"]
```

### 2.3 SOS 与通知事件链路

```mermaid
flowchart LR
    sos["SOS 按钮 / 倒计时确认 / 音频占位"] --> api["/api/trips/{id}/sos 或 /api/v2/sos"]
    alert["偏航 / 超时 / 行程开始 / 行程结束"] --> notify["notification_events.py"]
    api --> fastapi["FastAPI routers"]
    fastapi --> sosdb["ng_sos_event"]
    fastapi --> notify
    notify --> eventdb["ng_notification_event"]
```

## 3. 分层说明

- Android 客户端负责页面交互、地图展示、定位采集、前台服务保活、离线缓存与 API 调用。
- FastAPI 后端负责认证、行程管理、聊天消息、SOS 事件、通知事件记录与聚合。
- 数据持久化统一走 SQLAlchemy 模型；生产主库为 Neon Postgres，本地开发和演示可退回 SQLite。
- AI 对话优先调用 OpenAI；未配置 `OPENAI_API_KEY` 或外部请求失败时，后端会降级到本地规则回复。
- 通知模块当前主线是“记录通知事件并提供查询接口”，后续可在此层继续接入短信、推送或守护者小程序。
- 部署口径保持为 Android 客户端 + 独立 FastAPI 后端；Vercel 不承担主业务数据库持久化职责。

## 4. 对外展示时的推荐话术

可将 NyxGuard 概括为：

> 一个以 Android 端实时守护体验为前台、以 FastAPI 后端进行行程与事件编排、以 Neon Postgres 持久化核心安全数据、并通过 OpenAI 提供 AI 关怀能力的夜间出行安全系统。
